// Ensure Gradle can resolve plugins (used when running Gradle inside Docker)
pluginManagement {
	repositories {
		gradlePluginPortal()
		mavenCentral()
	}
}

rootProject.name = 'life-cycle-cell'

include 'application-service'
include 'notification-service'
include 'api-gateway'
